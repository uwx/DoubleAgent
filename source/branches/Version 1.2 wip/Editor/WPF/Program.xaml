<Application x:Class="AgentCharacterEditor.Program" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:custom="clr-namespace:DoubleAgent"
		StartupUri="MainWindow.xaml" Startup="Application_Startup">
	<Application.Resources>
		<ResourceDictionary>

			<Color x:Key="BackgroundLightColor" A="#FF" R="#B9" G="#D8" B="#F0" />
			<Color x:Key="BackgroundMediumColor" A="#FF" R="#64" G="#9D" B="#CF" />
			<Color x:Key="BackgroundDarkColor" A="#FF" R="#15" G="#48" B="#7B" />

			<SolidColorBrush x:Key="BackgroundLightBrush" Color="{StaticResource BackgroundLightColor}" />
			<SolidColorBrush x:Key="BackgroundMediumBrush" Color="{StaticResource BackgroundMediumColor}" />
			<SolidColorBrush x:Key="BackgroundDarkBrush" Color="{StaticResource BackgroundDarkColor}" />
			<SolidColorBrush x:Key="HeaderDarkBrush" Color="{StaticResource BackgroundDarkColor}" />

			<LinearGradientBrush x:Key="VerticalFade" StartPoint="0,0" EndPoint="0,1">
				<LinearGradientBrush.GradientStops>
					<GradientStop Color="{StaticResource BackgroundLightColor}" Offset="0" />
					<GradientStop Color="{StaticResource BackgroundMediumColor}" Offset="0.8" />
					<GradientStop Color="{StaticResource BackgroundMediumColor}" Offset="1" />
				</LinearGradientBrush.GradientStops>
			</LinearGradientBrush>

			<Style x:Key="x:MainGrid" TargetType="{x:Type Grid}">
				<Setter Property="Margin" Value="2" />
			</Style>

			<Style x:Key="{x:Type GroupBox}" TargetType="{x:Type GroupBox}">
				<Setter Property="Foreground" Value="{StaticResource HeaderDarkBrush}" />
				<Setter Property="BorderBrush" Value="{x:Null}" />
				<Setter Property="BorderThickness" Value="0" />
				<Setter Property="Margin" Value="4" />
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="{x:Type GroupBox}">
							<Grid SnapsToDevicePixels="true">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="10" />
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="10" />
								</Grid.ColumnDefinitions>
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto" />
									<RowDefinition Height="*" />
									<RowDefinition Height="10" />
								</Grid.RowDefinitions>
								<Rectangle RadiusX="10" RadiusY="10" Fill="{StaticResource BackgroundMediumBrush}"
										Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" Grid.ColumnSpan="3" />
								<Rectangle RadiusX="10" RadiusY="10" Fill="{StaticResource VerticalFade}"
										Margin="0,0,0,-10" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" />
								<ContentPresenter ContentSource="Header" Grid.Row="0" Grid.Column="1" Margin="2,2,2,7"
										VerticalAlignment="Top" HorizontalAlignment="Center" RecognizesAccessKey="True"
										TextBlock.FontSize="16"
										SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
								</ContentPresenter>
								<ContentPresenter Grid.Row="1" Grid.Column="1" Margin="{TemplateBinding Padding}"
										SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
								</ContentPresenter>
							</Grid>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>

			<Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="{x:Type Separator}">
				<Setter Property="OverridesDefaultStyle" Value="true" />
				<Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.MenuBrushKey}}" />
				<Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" />
				<Setter Property="BorderThickness" Value="1" />
				<Setter Property="Height" Value="1" />
				<Setter Property="Margin" Value="0,1,0,1" />
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="{x:Type Separator}">
							<Grid SnapsToDevicePixels="true">
								<Grid.ColumnDefinitions>
									<ColumnDefinition MinWidth="24" Width="Auto"
											SharedSizeGroup="MenuItemIconColumnGroup" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>
								<Border Grid.Column="1" Margin="8,0,0,0" Background="{TemplateBinding Background}"
										BorderBrush="{TemplateBinding BorderBrush}"
										BorderThickness="{TemplateBinding BorderThickness}" />
							</Grid>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>

			<Style x:Key="{x:Type ToolBar}" TargetType="{x:Type ToolBar}">
				<Setter Property="Background"
						Value="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ToolBarTray}, Path=Background}" />
			</Style>
			<Style x:Key="{ComponentResourceKey TypeInTargetAssembly={x:Type Thumb}, ResourceId=ToolBarThumbStyle}" TargetType="{x:Type Thumb}">
				<Setter Property="Thumb.Visibility" Value="Collapsed" />
			</Style>

			<Style x:Key="{x:Static ToolBar.ButtonStyleKey}" TargetType="{x:Type Button}">
				<Style.Triggers>
					<Trigger Property="IsEnabled" Value="false">
						<Setter Property="Opacity" Value="0.3" />
						<!--<Setter Property="Effect">
							<Setter.Value>
								<BlurEffect Radius="3.0" />
							</Setter.Value>
						</Setter>-->
					</Trigger>
				</Style.Triggers>
			</Style>

			<Style x:Key="NormalLabel" TargetType="{x:Type Label}">
				<Setter Property="Padding" Value="0,2,5,2"/>
				<Setter Property="Foreground" Value="GhostWhite"/>
			</Style>

			<Style x:Key="{x:Type custom:TextBoxEx}" TargetType="{x:Type custom:TextBoxEx}"
					BasedOn="{StaticResource {x:Type TextBox}}">
				<Setter Property="Margin" Value="2"/>
			</Style>

			<BitmapImage x:Key="ImgNavigateBack" UriSource="Resources/NavigateBack.png" />
			<BitmapImage x:Key="ImgNavigateForward" UriSource="Resources/NavigateForward.png" />
			<BitmapImage x:Key="ImgFileNew" UriSource="Resources/FileNew.png" />
			<BitmapImage x:Key="ImgFileOpen" UriSource="Resources/FileOpen.png" />
			<BitmapImage x:Key="ImgFileSave" UriSource="Resources/FileSave.png" />
			<BitmapImage x:Key="ImgFilePrint" UriSource="Resources/FilePrint.png" />
			<BitmapImage x:Key="ImgFilePrintPreview" UriSource="Resources/FilePrintPreview.png" />
			<BitmapImage x:Key="ImgEditCopy" UriSource="Resources/EditCopy.png" />
			<BitmapImage x:Key="ImgEditCut" UriSource="Resources/EditCut.png" />
			<BitmapImage x:Key="ImgEditDelete" UriSource="Resources/EditDelete.png" />
			<BitmapImage x:Key="ImgEditPaste" UriSource="Resources/EditPaste.png" />
			<BitmapImage x:Key="ImgEditUndo" UriSource="Resources/EditUndo.png" />
			<BitmapImage x:Key="ImgEditRedo" UriSource="Resources/EditRedo.png" />
			<BitmapImage x:Key="ImgHelpAbout" UriSource="Resources/About.png" />
			<BitmapImage x:Key="IconDoubleAgent" UriSource="Resources/DoubleAgent.ico" />
		</ResourceDictionary>
	</Application.Resources>
</Application>
